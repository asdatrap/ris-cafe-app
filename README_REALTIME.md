# 🍽️ Кафе приложение с реальным временем

## 📋 Обзор
Приложение теперь поддерживает **мгновенное обновление цен** в реальном времени через Server-Sent Events (SSE). Когда администратор меняет цены на сервере, все клиенты видят изменения мгновенно!

## 🚀 Новые возможности

### ✨ Реальное время
- **Зеленый индикатор** - подключено к SSE, цены обновляются мгновенно
- **Оранжевый индикатор** - работает автообновление каждые 30 секунд
- **Автопереподключение** при обрыве соединения

### 🔄 Как это работает
1. **Flutter приложение** подключается к `/api/events` (SSE)
2. **Администратор** меняет цену через админ-панель
3. **Сервер** отправляет уведомление всем подключенным клиентам
4. **Приложение** мгновенно обновляет цены в меню и корзине

## 🛠️ Запуск

### 1. Запуск сервера
```bash
# В папке cafe-backend
node glitch-server.js
```

### 2. Запуск Flutter приложения
```bash
# В папке windsurf-project
flutter run
```

## 📱 Админ-панель
Откройте в браузере: `http://localhost:3000`

- Изменяйте цены, названия товаров
- Добавляйте/удаляйте товары
- Все изменения мгновенно появятся в приложении

## 🔧 Тестирование

1. **Запустите приложение на двух устройствах**
2. **Откройте админ-панель**
3. **Измените цену любого товара**
4. **Наблюдайте мгновенное обновление** на обоих устройствах!

## 🌐 Работа через интернет

Для удаленного доступа:
1. Разверните сервер на Heroku, Vercel или другом хостинге
2. Обновите `api_config.dart` с URL вашего сервера
3. Приложение будет работать из любой точки мира

## 📊 Индикаторы в UI

### В шапке меню:
- 🟢 **Реальное время** - SSE подключен
- 🟠 **Автообновление** - Fallback режим

### В консоли:
- `Цена изменена: Капучино 109₸ → 120₸`
- `Новый товар: Моккачино`
- `Товар удален: Пирожное`

## 🔍 Отладка

Включите debug вывод в Flutter:
```dart
debugPrint('SSE Connected: $isRealTimeConnected');
```

Проверьте серверные логи:
```bash
🚀 Сервер запущен на порту 3000
📱 Админ-панель: http://localhost:3000
🔗 API меню: http://localhost:3000/api/menu
```

## 🚨 Возможные проблемы

### SSE не подключается
- Проверьте URL сервера в `api_config.dart`
- Убедитесь что порт не занят
- Проверьте CORS настройки

### Цены не обновляются
- Проверьте консоль браузера на ошибки
- Убедитесь что сервер запущен
- Перезапустите приложение

## 📝 Технические детали

### Server-Sent Events
```javascript
// SSE эндпоинт
app.get('/api/events', (req, res) => {
  // Отправка уведомлений клиентам
});
```

### Flutter SSE клиент
```dart
// Подключение к SSE
_sseSubscription = response.stream
    .transform(utf8.decoder)
    .listen(_handleSSEMessage);
```

### Fallback механизм
```dart
// Периодическое обновление если SSE не работает
Timer.periodic(Duration(seconds: 30), (_) {
  if (!_isRealTimeConnected) {
    _loadProducts();
  }
});
```

---

🎉 **Готово!** Ваше кафе приложение теперь обновляет цены в реальном времени!
